import { AppHighLight } from './app-high-light';

describe('AppHighLight', () => {
  it('should create an instance', () => {
    const directive = new AppHighLight();
    expect(directive).toBeTruthy();
  });
});
